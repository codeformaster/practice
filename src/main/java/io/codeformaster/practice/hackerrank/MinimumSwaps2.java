package io.codeformaster.practice.hackerrank;

import java.util.*;

public class MinimumSwaps2 {
    public static void main(String args[]) {
        int[] array = {9, 6, 4, 3, 5, 8, 1, 2, 7};
        System.out.println(minimumSwaps(array));
    }

    static int minimumSwaps(int[] arr) {

        return 0;
    }

    // Complete the minimumSwaps function below.
    static int minimumSwapsForSolution(int[] arr) {
//        int first = 0, last = arr.length - 1;
//        int swaps = 0;
//        while (first < last) {
//            while (arr[first] == first + 1 && first < last)
//                first++;
//            if (first < last) {
//                int temp = arr[arr[first] - 1];
//                arr[arr[first] - 1] = arr[first];
//                arr[first] = temp;
//                swaps++;
//            }
//        }
//        return swaps;
        return 0;
    }
}
